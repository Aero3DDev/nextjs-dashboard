import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* Chapter 10 - Enable PPR for your Next.js app
     The 'incremental' value allows you to adopt PPR for specific routes. */
  /*experimental: {
    ppr: 'incremental'
  }*/
};

export default nextConfig;
